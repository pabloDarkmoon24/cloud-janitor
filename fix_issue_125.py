Looking at this issue, I need to implement a redaction utility for the cloud-janitor project. The repo is primarily Java (Maven project) but the issue asks for Python implementation. I'll create a Python redaction module that can be used as a utility.

Let me analyze what's needed:
1. A redaction module that masks secrets/sensitive data
2. Integration with stdout/stderr and logging
3. Pattern matching for common secret types (passwords, tokens, API keys, etc.)

```json
{
  "solution_code": "# File: scripts/redact.py\nimport re\nimport sys\nimport logging\nimport os\nfrom typing import List, Tuple, Optional\n\n# Common patterns for secrets and sensitive data\nREDACTION_PATTERNS: List[Tuple[str, str]] = [\n    # AWS Keys\n    (r'(?i)(aws_?access_?key_?id[\\s]*[=:][\\s]*)[A-Z0-9]{20}', r'\\1[REDACTED_AWS_KEY_ID]'),\n    (r'(?i)(aws_?secret_?access_?key[\\s]*[=:][\\s]*)[A-Za-z0-9/+=]{40}', r'\\1[REDACTED_AWS_SECRET]'),\n    (r'(?i)(aws_?session_?token[\\s]*[=:][\\s]*)[A-Za-z0-9/+=]{16,}', r'\\1[REDACTED_AWS_TOKEN]'),\n    # Generic Passwords\n    (r'(?i)(password[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'  , r'\\1[REDACTED_PASSWORD]'),\n    (r'(?i)(passwd[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'    , r'\\1[REDACTED_PASSWORD]'),\n    (r'(?i)(pwd[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'       , r'\\1[REDACTED_PASSWORD]'),\n    # Tokens\n    (r'(?i)(token[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'     , r'\\1[REDACTED_TOKEN]'),\n    (r'(?i)(api_?key[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'  , r'\\1[REDACTED_API_KEY]'),\n    (r'(?i)(secret[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'    , r'\\1[REDACTED_SECRET]'),\n    # OAuth / Bearer tokens\n    (r'(?i)(bearer[\\s]+)[A-Za-z0-9\\-_\\.]+', r'\\1[REDACTED_BEARER_TOKEN]'),\n    (r'(?i)(authorization[\\s]*:[\\s]*)[^\\n]+'      , r'\\1[REDACTED_AUTH_HEADER]'),\n    # Private Keys\n    (r'-----BEGIN [A-Z ]*PRIVATE KEY-----[\\s\\S]*?-----END [A-Z ]*PRIVATE KEY-----',\n     '[REDACTED_PRIVATE_KEY]'),\n    # GitHub Tokens\n    (r'ghp_[A-Za-z0-9]{36}', '[REDACTED_GITHUB_TOKEN]'),\n    (r'gho_[A-Za-z0-9]{36}', '[REDACTED_GITHUB_TOKEN]'),\n    (r'github_pat_[A-Za-z0-9_]{82}', '[REDACTED_GITHUB_PAT]'),\n    # Generic high-entropy strings that look like secrets (base64-like, 32+ chars)\n    # Only redact if labeled\n    (r'(?i)(client_?secret[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'  , r'\\1[REDACTED_CLIENT_SECRET]'),\n    (r'(?i)(client_?id[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'      , r'\\1[REDACTED_CLIENT_ID]'),\n    # OpenShift / Kubernetes\n    (r'(?i)(pull.?secret[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'    , r'\\1[REDACTED_PULL_SECRET]'),\n    (r'(?i)(kubeconfig[\\s]*[=:][\\s]*)[^\\s,;\"\\']+'      , r'\\1[REDACTED_KUBECONFIG]'),\n    # SSH Keys\n    (r'-----BEGIN OPENSSH PRIVATE KEY-----[\\s\\S]*?-----END OPENSSH PRIVATE KEY-----',\n     '[REDACTED_SSH_PRIVATE_KEY]'),\n    # Generic connection strings\n    (r'(?i)(mongodb://)[^\\s]+', r'\\1[REDACTED_CONNECTION_STRING]'),\n    (r'(?i)(postgres://)[^\\s]+', r'\\1[REDACTED_CONNECTION_STRING]'),\n    (r'(?i)(mysql://)[^\\s]+', r'\\1[REDACTED_CONNECTION_STRING]'),\n    # JWT tokens\n    (r'eyJ[A-Za-z0-9\\-_]+\\.eyJ[A-Za-z0-9\\-_]+\\.[A-Za-z0-9\\-_.+/=]+',\n     '[REDACTED_JWT_TOKEN]'),\n]\n\n# Compile patterns for performance\nCOMPILED_PATTERNS = [\n    (re.compile(pattern), replacement)\n    for pattern, replacement in REDACTION_PATTERNS\n]\n\n# Additional secrets from environment variables to redact\n_ENV_SECRETS_CACHE: Optional[List[str]] = None\n\n\ndef _get_env_secrets() -> List[str]:\n    \"\"\"Collect secrets from well-known environment variables.\"\"\"\n    global _ENV_SECRETS_CACHE\n    if _ENV_SECRETS_CACHE is not None:\n        return _ENV_SECRETS_CACHE\n\n    secret_env_vars = [\n        'AWS_ACCESS_KEY_ID',\n        'AWS_SECRET_ACCESS_KEY',\n        'AWS_SESSION_TOKEN',\n        'GITHUB_TOKEN',\n        'GH_TOKEN',\n        'PULL_SECRET',\n        'KUBEADMIN_PASSWORD',\n        'OPENSHIFT_TOKEN',\n        'OPENSHIFT_PASSWORD',\n        'REDHAT_USERNAME',\n        'REDHAT_PASSWORD',\n        'CJ_PASSWORD',\n        'CJ_SECRET',\n        'CJ_TOKEN',\n        'CJ_API_KEY',\n        'DOCKER_PASSWORD',\n        'REGISTRY_PASSWORD',\n        'NPM_TOKEN',\n        'PYPI_TOKEN',\n        'SLACK_TOKEN',\n        'DISCORD_TOKEN',\n        'DATABASE_URL',\n        'DB_PASSWORD',\n    ]\n\n    secrets = []\n    for var in secret_env_vars:\n        value = os.environ.get(var)\n        if value and len(value) > 3:  # Skip very short values to avoid false positives\n            secrets.append(value)\n\n    # Also look for any env var that looks like a secret by name\n    secret_keywords = ['secret', 'password', 'passwd', 'token', 'api_key', 'apikey',\n                       'private_key', 'privatekey', 'credentials', 'auth']\n    for key, value in os.environ.items():\n        if value and len(value) > 8:\n            key_lower = key.lower()\n            if any(kw in key_lower for kw in secret_keywords):\n                secrets.append(value)\n\n    _ENV_SECRETS_CACHE = list(set(secrets))  # Deduplicate\n    return _ENV_SECRETS_CACHE\n\n\ndef redact(text: str) -> str:\n    \"\"\"\n    Redact sensitive information from a string.\n    \n    Args:\n        text: The string to redact sensitive information from.\n    \n    Returns:\n        The redacted string with sensitive information replaced.\n    \"\"\"\n    if not text:\n        return text\n\n    result = text\n\n    # First, redact known env variable values\n    for secret in _get_env_secrets():\n        if secret in result:\n            result = result.replace(secret, '[REDACTED_SECRET]')\n\n    # Then apply regex patterns\n    for pattern, replacement in COMPILED_PATTERNS:\n        result = pattern.sub(replacement, result)\n\n    return result\n\n\ndef invalidate_env_cache():\n    \"\"\"Invalidate the environment secrets cache (useful for testing).\"\"\"\n    global _ENV_SECRETS_CACHE\n    _ENV_SECRETS_CACHE = None\n\n\nclass RedactingStream:\n    \"\"\"\n    A stream wrapper that redacts sensitive information before writing.\n    Can be used to wrap sys.stdout or sys.stderr.\n    \"\"\"\n\n    def __init__(self, stream):\n        self._stream = stream\n\n    def write(self, text: str) -> int:\n        redacted = redact(str(text))\n        return self._stream.write(redacted)\n\n    def writelines(self, lines):\n        redacted_lines = [redact(str(line)) for line in lines]\n        return self._stream.writelines(redacted_lines)\n\n    def flush(self):\n        return self._stream.flush()\n\n    def fileno(self):\n        return self._stream.fileno()\n\n    def isatty(self):\n        return self._stream.isatty()\n\n    def __getattr__(self, name):\n        return getattr(self._stream, name)\n\n\nclass RedactingFormatter(logging.Formatter):\n    \"\"\"\n    A logging formatter that redacts sensitive information from log records.\n    \"\"\"\n\n    def __init__(self, fmt=None, datefmt=None, style='%', validate=True):\n        super().__init__(fmt=fmt, datefmt=datefmt, style=style, validate=validate)\n\n    def format(self, record: logging.LogRecord) -> str:\n        # Redact the message and its arguments before formatting\n        original_msg = record.msg\n        original_args = record.args\n\n        try:\n            if isinstance(record.msg, str):\n                record.msg = redact(record.msg)\n            \n            if record.args:\n                if isinstance(record.args, dict):\n                    record.args = {\n                        k: redact(str(v)) if isinstance(v, str) else v\n                        for k, v in record.args.items()\n                    }\n                elif isinstance(record.args, (list, tuple)):\n                    record.args = tuple(\n                        redact(str(a)) if isinstance(a, str) else a\n                        for a in record.args\n                    )\n\n            formatted = super().format(record)\n            return redact(formatted)  # Final pass to catch anything missed\n        finally:\n            # Restore original values to avoid side effects\n            record.msg = original_msg\n            record.args = original_args\n\n\ndef install_redacting_streams():\n    \"\"\"\n    Replace sys.stdout and sys.stderr with redacting versions.\n    Call this at application startup to enable output redaction.\n    \"\"\"\n    if not isinstance(sys.stdout, RedactingStream):\n        sys.stdout = RedactingStream(sys.stdout)\n