```json
{
  "solution_code": "### File: src/main/python/cloudjanitor/tasks/ec2_waste_detection.py\n\nimport boto3\nimport json\nimport logging\nfrom datetime import datetime, timedelta\nfrom typing import Optional\n\nlogger = logging.getLogger(__name__)\n\n\nclass EC2WasteDetector:\n    \"\"\"\n    Detects waste on EC2 instances using AWS Compute Optimizer and CloudWatch.\n    Identifies overprovisioned instances and generates recommendations.\n    \"\"\"\n\n    def __init__(\n        self,\n        region: Optional[str] = None,\n        cpu_threshold: float = 10.0,\n        memory_threshold: float = 20.0,\n        lookback_days: int = 14,\n        dry_run: bool = True,\n    ):\n        \"\"\"\n        Initialize the EC2 waste detector.\n\n        Args:\n            region: AWS region to analyze (defaults to session default)\n            cpu_threshold: CPU utilization % below which instance is considered overprovisioned\n            memory_threshold: Memory utilization % below which instance is considered overprovisioned\n            lookback_days: Number of days to analyze CloudWatch metrics\n            dry_run: If True, only report findings without taking action\n        \"\"\"\n        self.region = region\n        self.cpu_threshold = cpu_threshold\n        self.memory_threshold = memory_threshold\n        self.lookback_days = lookback_days\n        self.dry_run = dry_run\n\n        session = boto3.Session(region_name=region) if region else boto3.Session()\n        self.ec2_client = session.client(\"ec2\")\n        self.cloudwatch_client = session.client(\"cloudwatch\")\n        self.compute_optimizer_client = session.client(\"compute-optimizer\")\n        self.pricing_client = boto3.client(\"pricing\", region_name=\"us-east-1\")\n\n    def get_running_instances(self) -> list:\n        \"\"\"Retrieve all running EC2 instances.\"\"\"\n        instances = []\n        paginator = self.ec2_client.get_paginator(\"describe_instances\")\n        pages = paginator.paginate(\n            Filters=[{\"Name\": \"instance-state-name\", \"Values\": [\"running\"]}]\n        )\n        for page in pages:\n            for reservation in page[\"Reservations\"]:\n                for instance in reservation[\"Instances\"]:\n                    instances.append(instance)\n        logger.info(f\"Found {len(instances)} running EC2 instances\")\n        return instances\n\n    def get_cloudwatch_cpu_metrics(self, instance_id: str) -> dict:\n        \"\"\"\n        Get CPU utilization metrics from CloudWatch for an instance.\n\n        Returns dict with avg, max, min CPU utilization percentages.\n        \"\"\"\n        end_time = datetime.utcnow()\n        start_time = end_time - timedelta(days=self.lookback_days)\n\n        try:\n            response = self.cloudwatch_client.get_metric_statistics(\n                Namespace=\"AWS/EC2\",\n                MetricName=\"CPUUtilization\",\n                Dimensions=[{\"Name\": \"InstanceId\", \"Value\": instance_id}],\n                StartTime=start_time,\n                EndTime=end_time,\n                Period=3600,  # 1-hour periods\n                Statistics=[\"Average\", \"Maximum\", \"Minimum\"],\n            )\n\n            datapoints = response.get(\"Datapoints\", [])\n            if not datapoints:\n                return {\"avg\": None, \"max\": None, \"min\": None, \"datapoints\": 0}\n\n            avg_cpu = sum(dp[\"Average\"] for dp in datapoints) / len(datapoints)\n            max_cpu = max(dp[\"Maximum\"] for dp in datapoints)\n            min_cpu = min(dp[\"Minimum\"] for dp in datapoints)\n\n            return {\n                \"avg\": round(avg_cpu, 2),\n                \"max\": round(max_cpu, 2),\n                \"min\": round(min_cpu, 2),\n                \"datapoints\": len(datapoints),\n            }\n        except Exception as e:\n            logger.warning(f\"Could not get CPU metrics for {instance_id}: {e}\")\n            return {\"avg\": None, \"max\": None, \"min\": None, \"datapoints\": 0}\n\n    def get_cloudwatch_memory_metrics(self, instance_id: str) -> dict:\n        \"\"\"\n        Get memory utilization metrics from CloudWatch for an instance.\n        Note: Requires CloudWatch agent to be installed on the instance.\n\n        Returns dict with avg, max memory utilization percentages.\n        \"\"\"\n        end_time = datetime.utcnow()\n        start_time = end_time - timedelta(days=self.lookback_days)\n\n        try:\n            response = self.cloudwatch_client.get_metric_statistics(\n                Namespace=\"CWAgent\",\n                MetricName=\"mem_used_percent\",\n                Dimensions=[{\"Name\": \"InstanceId\", \"Value\": instance_id}],\n                StartTime=start_time,\n                EndTime=end_time,\n                Period=3600,\n                Statistics=[\"Average\", \"Maximum\"],\n            )\n\n            datapoints = response.get(\"Datapoints\", [])\n            if not datapoints:\n                # Try alternate namespace used by some agents\n                response = self.cloudwatch_client.get_metric_statistics(\n                    Namespace=\"System/Linux\",\n                    MetricName=\"MemoryUtilization\",\n                    Dimensions=[{\"Name\": \"InstanceId\", \"Value\": instance_id}],\n                    StartTime=start_time,\n                    EndTime=end_time,\n                    Period=3600,\n                    Statistics=[\"Average\", \"Maximum\"],\n                )\n                datapoints = response.get(\"Datapoints\", [])\n\n            if not datapoints:\n                return {\"avg\": None, \"max\": None, \"available\": False}\n\n            avg_mem = sum(dp[\"Average\"] for dp in datapoints) / len(datapoints)\n            max_mem = max(dp[\"Maximum\"] for dp in datapoints)\n\n            return {\n                \"avg\": round(avg_mem, 2),\n                \"max\": round(max_mem, 2),\n                \"available\": True,\n            }\n        except Exception as e:\n            logger.warning(f\"Could not get memory metrics for {instance_id}: {e}\")\n            return {\"avg\": None, \"max\": None, \"available\": False}\n\n    def get_compute_optimizer_recommendations(self, instance_arns: list) -> dict:\n        \"\"\"\n        Fetch EC2 instance recommendations from AWS Compute Optimizer.\n\n        Returns a dict mapping instance_id -> recommendation details.\n        \"\"\"\n        recommendations = {}\n\n        try:\n            paginator = self.compute_optimizer_client.get_paginator(\n                \"get_ec2_instance_recommendations\"\n            )\n            pages = paginator.paginate(instanceArns=instance_arns)\n\n            for page in pages:\n                for rec in page.get(\"instanceRecommendations\", []):\n                    instance_id = rec[\"instanceArn\"].split(\"/\")[-1]\n                    finding = rec.get(\"finding\", \"UNKNOWN\")\n                    recommendation_options = rec.get(\"recommendationOptions\", [])\n\n                    current_type = rec.get(\"currentInstanceType\", \"unknown\")\n                    recommended_types = [\n                        opt.get(\"instanceType\", \"unknown\")\n                        for opt in recommendation_options\n                    ]\n\n                    # Extract performance risk and savings\n                    savings_opportunity = None\n                    if recommendation_options:\n                        top_rec = recommendation_options[0]\n                        savings_opportunity = top_rec.get(\n                            \"savingsOpportunity\", {}\n                        ).get(\"estimatedMonthlySavings\", {}).get(\"value\")\n\n                    recommendations[instance_id] = {\n                        \"finding\": finding,\n                        \"current_type\": current_type,\n                        \"recommended_types\": recommended_types,\n                        \"estimated_monthly_savings\": savings_opportunity,\n                        \"utilization_metrics\": rec.get(\"utilizationMetrics\", []),\n                    }\n\n            # Handle errors from Compute Optimizer\n            for error in page.get(\"errors\", []):\n                instance_id = error.get(\"instanceArn\", \"\").split(\"/\")[-1]\n                logger.warning(\n                    f\"Compute Optimizer error for {instance_id}: {error.get('message')}\"\n                )\n\n        except self.compute_optimizer_client.exceptions.OptInRequiredException:\n            logger.warning(\n                \"AWS Compute Optimizer is not enabled. \"\n                \"Enable it at https://console.aws.amazon.com/compute-optimizer/\"\n            )\n        except Exception as e:\n            logger.warning(f\"Could not fetch Compute Optimizer recommendations: {e}\")\n\n        return recommendations\n\n    def get_instance_name(self, instance: dict) -> str:\n        \"\"\"Extract instance name from tags.\"\"\"\n        tags = instance.get(\"Tags\", [])\n        for tag in tags:\n            if tag[\"Key\"] == \"Name\":\n                return tag[\"Value\"]\n        return \"(unnamed)\"\n\n    def calculate_waste_score(self, cpu_metrics: dict, memory_metrics: dict, optimizer_finding: str) -> dict:\n        \"\"\"\n        Calculate a waste score and determination for an instance.\n\n        Returns dict with is_waste bool, severity, and reasons.\n        \"\"\"\n        reasons = []\n        severity = \"NONE\"\n        is_waste = False\n\n        # Check CPU\n        if cpu_metrics.get(\"avg\") is not None:\n            avg_cpu = cpu_metrics[\"avg\"]\n            max_cpu = cpu_metrics[\"max\"]\n            if avg_cpu < self.cpu_threshold:\n                reasons.append(\n                    f\"Low avg CPU utilization: {avg_cpu}% (threshold: {self.cpu_threshold}%)\"\n                )\n                is_waste = True\n                severity = \"MEDIUM\"\n            if max_cpu is not None and max_cpu < (self.cpu_threshold * 2):\n                reasons.append(\n                    f\"Low max CPU utilization: {max_cpu}% over {self.lookback_days} days\"\n                )\n\n        # Check Memory\n        if memory_metrics.get(\"available\") and memory_metrics.get(\"avg\") is not None:\n            avg_mem = memory_metrics[\"avg\"]\n            if avg_mem < self.memory_threshold:\n                reasons.append(\n                    f\"Low avg memory utilization: {avg_mem}% (threshold: {self.memory_threshold}%)\"\n                )\n                is_waste = True\n                if severity == \"MEDIUM\":\n                    severity = \"HIGH\"\n                else:\n                    severity = \"MEDIUM\"\n\n        # Incorporate Compute Optimizer finding\n        if optimizer_finding in [\"OVER_PROVISIONED\"]:\n            reasons.append(f\"AWS Compute Optimizer